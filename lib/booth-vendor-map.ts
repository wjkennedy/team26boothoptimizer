/**
 * Manual mapping of booth IDs to vendor/company names.
 * This data comes from ExpoFP's exhibitor list but requires API authentication.
 * Update this map as vendor information becomes available.
 * 
 * Format: "BOOTH_ID": "Vendor Name"
 */

export const boothVendorMap: Record<string, string> = {
  // Add mappings as they become available from ExpoFP
  // Example:
  // "1117": "Atlassian",
  // "1123": "CloudBees",
}
